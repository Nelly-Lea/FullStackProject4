import React, { Component } from 'react';

class Options extends Component {
    state = {  } 

    //list of all options

    render() { 
        return (
            <div></div>
            //forach element in options : 
            //add it to div by add a OptionButton tag with props value of "option" = element of options list
        );
    }
}
 
export default Options;